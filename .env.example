# Copy to .env (local dev) or .env.production (build).
#
# Local dev (with Vite proxy in vite.config.js):
#   VITE_API_BASE=/arusuvai
#
# Production (Render):
#   VITE_API_BASE=https://arusuvaijunctionbackend.onrender.com/arusuvai
VITE_API_BASE=/arusuvai
